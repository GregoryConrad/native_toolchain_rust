name: dart_only_example
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  hooks: ^2.1.0
  native_toolchain_rust: ^1.0.5

dev_dependencies:
  ffigen: ^21.0.0
  test: ^1.29.0
