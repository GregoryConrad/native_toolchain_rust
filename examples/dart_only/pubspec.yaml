name: dart_only_example
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  hooks: ^2.0.0
  native_toolchain_rust: ^1.0.4

dev_dependencies:
  ffigen: ^20.1.0
  test: ^1.31.0
